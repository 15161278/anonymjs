prototype = _.extend({
    /**
     * Filter the DOM element(s).
     * @method filter
     * @param {String} selector The css selector.
     * @return {Element}
     * @chainable
     * @example
     *     var element = <%= pkg.name %>.Element(selector);
     *     element.filter('.class');
     */
    filter: function (selector) {
        var self = this;
    }
}, prototype);